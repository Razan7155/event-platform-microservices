import { createTheme } from "@mui/material/styles";

const theme = createTheme({

  palette: {

    mode: "dark",

    primary: {
      main: "#4f46e5"
    },

    background: {
      default: "#020617",
      paper: "#0f172a"
    }

  }

});

export default theme;